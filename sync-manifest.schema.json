{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://fro.bot/systematic/sync-manifest.schema.json",
  "title": "Systematic Sync Manifest",
  "description": "Provenance manifest tracking upstream sources for bundled definitions, including intelligent rewrite tracking for repeatable conversions",
  "type": "object",
  "required": ["sources", "definitions"],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "sources": {
      "type": "object",
      "description": "Named upstream source repositories",
      "additionalProperties": {
        "type": "object",
        "required": ["repo", "branch", "url"],
        "properties": {
          "repo": {
            "type": "string",
            "description": "GitHub owner/repo"
          },
          "branch": {
            "type": "string",
            "description": "Default branch name"
          },
          "url": {
            "type": "string",
            "description": "Full repository URL",
            "format": "uri"
          }
        },
        "additionalProperties": false
      }
    },
    "definitions": {
      "type": "object",
      "description": "Bundled definitions mapped to their upstream provenance and conversion metadata",
      "additionalProperties": {
        "type": "object",
        "required": [
          "source",
          "upstream_path",
          "upstream_commit",
          "synced_at",
          "notes"
        ],
        "properties": {
          "source": {
            "type": "string",
            "description": "Key into sources map"
          },
          "upstream_path": {
            "type": "string",
            "description": "File path within source repo"
          },
          "upstream_commit": {
            "type": "string",
            "description": "Commit SHA at time of sync"
          },
          "synced_at": {
            "type": "string",
            "description": "ISO 8601 timestamp of last sync",
            "format": "date-time"
          },
          "notes": {
            "type": "string",
            "description": "Human-readable summary of what was changed or adapted during import"
          },
          "upstream_content_hash": {
            "type": "string",
            "description": "SHA-256 hash of the upstream file content at time of sync, for detecting upstream changes"
          },
          "rewrites": {
            "type": "array",
            "description": "Structured log of intelligent rewrites applied beyond mechanical conversion. Each entry records what was changed and why, enabling repeatable re-application on future syncs.",
            "items": {
              "type": "object",
              "required": ["field", "reason"],
              "properties": {
                "field": {
                  "type": "string",
                  "description": "Frontmatter field or content section that was rewritten (e.g., 'description', 'body:tool-references', 'body:branding')"
                },
                "reason": {
                  "type": "string",
                  "description": "Why this rewrite was needed (e.g., 'CC description lacks OC trigger conditions', 'branding reference to Claude Code')"
                },
                "original": {
                  "type": "string",
                  "description": "Original value or snippet before rewrite (optional, for diffing)"
                }
              },
              "additionalProperties": false
            }
          },
          "manual_overrides": {
            "type": "array",
            "description": "Fields or sections that have been manually customized by a human contributor. These MUST be preserved on future re-imports — the skill will skip rewriting these fields.",
            "items": {
              "type": "string"
            }
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
